import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "OAK Foundation Partner Gathering | 9-11 November 2026, Harare",
  description:
    "Registration for the OAK Zimbabwe Foundation Partner Gathering, 9-11 November 2026 at Cresta Lodge, Msasa, Harare.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full bg-oak-background text-oak-text">{children}</body>
    </html>
  );
}

